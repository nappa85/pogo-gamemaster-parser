name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: GitHub Pages Deploy
      uses: maxheld83/ghpages@v0.2.1
      secrets:
        - "GH_PAT"
      env:
        BUILD_DIR: .
